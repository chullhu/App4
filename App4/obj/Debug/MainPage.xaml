<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App4"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converter="using:App4"
    xmlns:ObjDay="using:App4.ObjDay"
    x:Class="App4.MainPage"
    mc:Ignorable="d" Background="Black">

    <Page.Resources>
        <converter:DataConv x:Key="DataConv"/>
    </Page.Resources>

    <Page.BottomAppBar>
        <CommandBar x:Name="bar" ClosedDisplayMode="Minimal" Background="#FFF5CC4E">
            <CommandBar.SecondaryCommands>
                <AppBarToggleButton Label="settings"/>
                <AppBarToggleButton Label="about"/>
            </CommandBar.SecondaryCommands>
            <AppBarButton x:ConnectionId='1' x:Name="location" Label="location"                       />
            <AppBarButton x:ConnectionId='2' x:Name="refresh" Label="refresh"                      />
        </CommandBar>
    </Page.BottomAppBar>

    <!--<phone:PhoneApplicationPage.Resources >
        <converter:DataConv xmlns:converter="using:App4" x:Key="DataConv"></converter:DataConv>
    </phone:PhoneApplicationPage.Resources>-->
    <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0" Background="#FF462544">
        <StackPanel>
            <StackPanel x:Name="AddCity">
                <TextBlock Text="Enter city" FontSize="20" Margin="5"/>
                <TextBox x:Name="txtPincode"/>
                <Button x:ConnectionId='3' x:Name="btnGetData" Content="Save"                          HorizontalAlignment="Center" />
                <ProgressBar IsIndeterminate="true" Visibility="Collapsed" x:Name="pbWeather" />
            </StackPanel>

            <StackPanel>
                <StackPanel.DataContext>
                    <ObjDay:DayClass/>
                </StackPanel.DataContext>
                <TextBlock Text="{Binding CurrentDay.temp.day}" FontSize="20"/>
            </StackPanel>
            
            <StackPanel x:Name="spWeatherInfo" Margin="0,50,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="4" x:Name="txtCity" Text="{Binding city.name}"/>
                    <TextBlock Margin="4" Text="{Binding city.country}"/>
                </StackPanel>
                <StackPanel>
                    <ListBox ItemsSource="{Binding list}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="8" >
                                    <StackPanel Orientation="Horizontal" Margin="2">
                                        <TextBlock Text="{Binding dt, Converter={StaticResource DataConv}}"  Margin="2" />
                                        <TextBlock Text="{Binding weather[0].description}" Margin="2" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="2">
                                        <TextBlock Margin="2">
											<Run Text="Min Temp"/>
                                        </TextBlock>
                                        <TextBlock Margin="2" Text="{Binding temp.min}" />
                                        <TextBlock Margin="2">
											<Run Text="Max Temp"/>
                                        </TextBlock>
                                        <TextBlock Margin="2" Text="{Binding temp.max}" />
                                        <TextBlock Text="Speed" Margin="2"/>
                                        <TextBlock Text="{Binding speed}"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Grid>

</Page>

